import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  root: {
    marginBottom: "20px",
    padding: "20px",
  },
  loading: {
    marginBottom: "20px",
    padding: "20px",
  },
  title: {
    fontSize: "16pt",
  },
  pos: {
    marginBottom: 12,
  },
});

export default useStyles;
